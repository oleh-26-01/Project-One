<UserControl x:Class="Project_One.Controls.SecondTopPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="33" d:DesignWidth="1010">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Themes/GlobalStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <!-- Removed local CheckBoxStyle and LabelStyle -->
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid ShowGridLines="False" Background="{StaticResource PrimaryBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.5*" />
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0" Background="{StaticResource ListItemBackgroundBrush}" Margin="0,0,1,0">
            <CheckBox x:Name="FollowCarCheckBox" 
                      Style="{StaticResource BaseCheckBoxStyle}"
                      Content="Follow Car" IsChecked="True" 
                      FlowDirection="RightToLeft"
                      HorizontalAlignment="Center"/>
        </Grid>
        <Grid Grid.Column="1" Background="{StaticResource ListItemBackgroundBrush}" Margin="0,0,1,0">
            <CheckBox x:Name="ShowVisionCheckBox" 
                      Style="{StaticResource BaseCheckBoxStyle}"
                      Content="Show Intersections" IsChecked="True" 
                      FlowDirection="RightToLeft"
                      HorizontalAlignment="Center"/>
        </Grid>

        <Grid Grid.Column="2" Background="{StaticResource ListItemBackgroundBrush}" Margin="0,0,1,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Car Direction: " 
                       Style="{StaticResource BaseTextBlockStyle}"
                       HorizontalAlignment="Center"/>
            <TextBlock Grid.Column="1" x:Name="CarDirectionLabel" 
                       Style="{StaticResource BaseTextBlockStyle}" 
                       Text="Forward"
                       Foreground="{StaticResource AccentBrush}"
                       HorizontalAlignment="Center"/>
        </Grid>

        <Grid Grid.Column="3" Background="{StaticResource ListItemBackgroundBrush}" Margin="0,0,1,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Collision: " 
                       Style="{StaticResource BaseTextBlockStyle}"
                       HorizontalAlignment="Center"/>
            <TextBlock Grid.Column="1" x:Name="CollisionLabel" 
                       Style="{StaticResource BaseTextBlockStyle}" 
                       Text="None"
                       Foreground="{StaticResource AccentBrush}"
                       HorizontalAlignment="Center"/>
        </Grid>

        <Grid Grid.Column="4" Background="{StaticResource ListItemBackgroundBrush}" Margin="0 0 1 0">
            <TextBlock x:Name="CpsLabel"
                       Style="{StaticResource BaseTextBlockStyle}"
                       Text="Cps: 25000.0"
                       ToolTip="Calculations per second"
                       HorizontalAlignment="Center"/>
        </Grid>
    </Grid>
</UserControl>